int pin=11;
int state=0;
int relay=13;
void setup()
{
pinMode(pin,input);
pinMode(relay,OUTPUT);
}
void loop()
{
state=digitalRead(pin);
if(state==1)
{digitalWrite(relay,1);
}
else
{
digitalWrite(relay,0);
}
}
